---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

const currentPath = Astro.url.pathname;
const lang = 'id';

const title = 'Syaiful Anam a.k.a syanampro - Lifelong Builder';
const description = 'A lifelong builder applying learnings in tech and business, contributing to Digital360 and Fabrik Group with AI-driven development.';
---

<Layout title={title} description={description} lang={lang}>
	<div class="container">
		<Header currentPath={currentPath} lang={lang} />

		<main class="main-content">
			<div class="hero-section">
				<img
					src="/images/profile/syanampro.webp"
					alt="Syaiful Anam"
					class="profile-image"
					width="300"
					height="300"
				/>

				<h1 class="hero-heading">👋 Hai, Saya Syaiful Anam</h1>
				<p class="hero-aka">a.k.a. syanampro</p>
				<p class="hero-subtitle">
					A lifelong builder. Applying my learnings in tech and business.
				</p>

				<div class="content-boxes">
					<div class="content-box">
						<h2>Berkontribusi di Digital360</h2>
						<p>
							Membangun solusi software B2B generasi berikutnya dengan
							keahlian full-stack dan AI untuk mempercepat inovasi produk
							bagi klien enterprise.
						</p>
						<a href="https://digital360.id/" target="_blank" rel="noopener noreferrer" class="cta-link">
							Lihat Karya Kami →
						</a>
					</div>

					<div class="content-box">
						<h2>Berkontribusi di Fabrik Group</h2>
						<p>
							Mengintegrasikan pendekatan *tech-first* ke industri garment.
							Bekerja dengan founder brand fashion untuk menyederhanakan
							operasional dan implementasi solusi digital.
						</p>
						<a href="https://fabrikgroup.id/" target="_blank" rel="noopener noreferrer" class="cta-link">
							Jelajahi Brand Kami →
						</a>
					</div>
				</div>

				<div class="focus-section">
					<h2>Fokus Utama: Pengembangan Berbasis AI</h2>
					<p>
						Memanfaatkan kecerdasan buatan sebagai alat utama di seluruh
						perusahaan untuk membangun, menguji, dan implementasikan
						solusi perangkat lunak lebih efisien.
					</p>
				</div>
			</div>
		</main>
		<Footer lang={lang} />
	</div>
</Layout>

<style>
	.main-content {
		min-height: calc(100vh - 200px);
	}

	.hero-section {
		text-align: center;
		padding: var(--spacing-xl) 0;
	}

	.profile-image {
		width: 300px;
		height: 300px;
		border-radius: 50%;
		margin-bottom: var(--spacing-md);
		border: 2px solid var(--color-border);
		transition: border-color 0.3s ease;
	}

	.profile-image:hover {
		border-color: var(--color-text);
	}

	.hero-aka {
		font-size: 1rem;
		color: var(--color-text-light);
		margin-bottom: var(--spacing-md);
		font-weight: 400;
		font-style: italic;
	}

	.hero-subtitle {
		font-size: 1.125rem;
		color: var(--color-text-secondary);
		margin-bottom: var(--spacing-2xl);
		font-weight: 400;
		line-height: 1.5;
	}

	.content-boxes {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
		gap: var(--spacing-lg);
		margin-bottom: var(--spacing-lg);
		text-align: left;
	}

	.content-box {
		background-color: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: 6px;
		padding: var(--spacing-lg);
		transition: border-color 0.2s ease;
		position: relative;
		overflow: hidden;
		display: flex;
		flex-direction: column;
	}

	.content-box:hover {
		border-color: var(--color-text);
	}

	.content-box h2 {
		font-size: 1.25rem;
		margin-top: 0;
		margin-bottom: var(--spacing-md);
	}

	.content-box p {
		margin-bottom: var(--spacing-lg);
		color: var(--color-text-secondary);
		line-height: 1.6;
		flex-grow: 1;
	}

	.cta-link {
		display: inline-flex;
		align-items: center;
		color: var(--color-text-secondary);
		font-weight: 400;
		text-decoration: none;
		transition: color 0.2s ease;
		font-size: 0.9375rem;
		margin-top: auto;
	}

	.cta-link:hover {
		color: var(--color-text);
	}

	.focus-section {
		background-color: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: 6px;
		padding: var(--spacing-lg);
		text-align: center;
		max-width: 600px;
		margin: 0 auto;
	}

	.focus-section h2 {
		font-size: 1.5rem;
		margin-top: 0;
		margin-bottom: var(--spacing-md);
	}

	.focus-section p {
		font-size: 1.125rem;
		color: var(--color-text-secondary);
		margin-bottom: 0;
	}

	.focus-section .detail-text {
		line-height: 1.6;
		text-align: center;
	}

	/* Responsive design */
	@media (max-width: 768px) {
		.hero-section {
			padding: var(--spacing-lg) 0;
		}

		.profile-image {
			width: 200px;
			height: 200px;
			margin-bottom: var(--spacing-sm);
		}

		.hero-subtitle {
			font-size: 1.125rem;
			margin-bottom: var(--spacing-xl);
		}

		.content-boxes {
			grid-template-columns: 1fr;
			gap: var(--spacing-md);
		}

		.content-box {
			padding: var(--spacing-lg);
		}

		.focus-section {
			padding: var(--spacing-lg);
			text-align: left;
		}

		/* Hero heading responsive */
		.hero-heading {
			white-space: nowrap;
		}

		@media (max-width: 768px) {
			.hero-heading {
				white-space: normal;
			}
		}

		.content-box h2,
		.focus-section h2 {
			font-size: 1.125rem;
		}
	}
</style>